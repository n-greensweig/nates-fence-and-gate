version: 1
frontend:
  phases:
    preBuild:
      commands:
        - git pull origin main   # Ensure the latest code is fetched
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*  
